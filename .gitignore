# Server IP, root user, and SSH key path - kept out of history at Chris's
# request, even though this repo is local-only (no remote configured).
/vps-access.md

# Python bytecode cache generated by tools/vps.py imports.
/tools/__pycache__/
